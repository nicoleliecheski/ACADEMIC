# Conversor de Moedas (MVC)

Aplicação web implementada com o padrão MVC, usando Python e Flask.

## Como rodar

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

Então acesse http://127.0.0.1:5000
