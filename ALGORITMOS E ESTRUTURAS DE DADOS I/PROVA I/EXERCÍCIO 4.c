#include <stdio.h>
#include <string.h>

int c = 0;
char P[260][]

void perm(char S[260]){ // função para implementar a recursividade
    
}

int main (){
    int i;
    char S[260];

    memset(M, -1, sizeof(M)); // definindo todos os espaços da matriz como -1 (serve para verificar se aquela posição já foi preenchida)

    fgets(S, 260, stdin); //  entrada

    perm(S); // chamo a a funçao pela primeira vez

    for (i = 0; i < c; i++) // impressao dos anagramas
    {
        
    }   

    return 0;
}
