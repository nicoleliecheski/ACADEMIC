#include <stdio.h>

int main() {
    int N, S, X[N], r, i;
    scanf("%d\n%d\n", &N, &S);
    for (int c = 0; c < N; c++){
        scanf("%d\n", X[c]);
    }
    
    for (int c = 0; c < N; c++){
        for (){
            r = X[c] + X[c+c];
            if (r == S){
                i = i++;
            }
        }
    }
    
    return 0;

}
